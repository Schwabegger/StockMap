# StockMap configuration
# Copy this file to .env and edit the values

# Server port
PORT=3000

# Admin account — full read + write access
ADMIN_USER=admin
ADMIN_PASS=stockmap

# Guest account — read-only access
GUEST_USER=guest
GUEST_PASS=readonly
